import {
  require_react
} from "./chunk-V6FWWDGV.js";
import {
  __toESM
} from "./chunk-5CYG367S.js";

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useDebouncedCallback/useDebouncedCallback.js
var import_react2 = __toESM(require_react());

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useUnmountEffect/useUnmountEffect.js
var import_react = __toESM(require_react());
function useUnmountEffect(effect) {
  (0, import_react.useEffect)(function() {
    return function() {
      effect();
    };
  }, []);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useDebouncedCallback/useDebouncedCallback.js
var __spreadArray = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function useDebouncedCallback(callback, deps, delay, maxWait) {
  if (maxWait === void 0) {
    maxWait = 0;
  }
  var timeout = (0, import_react2.useRef)();
  var waitTimeout = (0, import_react2.useRef)();
  var lastCall = (0, import_react2.useRef)();
  var clear = function() {
    if (timeout.current) {
      clearTimeout(timeout.current);
      timeout.current = void 0;
    }
    if (waitTimeout.current) {
      clearTimeout(waitTimeout.current);
      waitTimeout.current = void 0;
    }
  };
  useUnmountEffect(clear);
  return (0, import_react2.useMemo)(function() {
    var execute = function() {
      if (!lastCall.current)
        return;
      var context = lastCall.current;
      lastCall.current = void 0;
      callback.apply(context.this, context.args);
      clear();
    };
    var wrapped = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      if (timeout.current) {
        clearTimeout(timeout.current);
      }
      lastCall.current = { args, this: this };
      timeout.current = setTimeout(execute, delay);
      if (maxWait > 0 && !waitTimeout.current) {
        waitTimeout.current = setTimeout(execute, maxWait);
      }
    };
    Object.defineProperties(wrapped, {
      length: { value: callback.length },
      name: { value: "".concat(callback.name || "anonymous", "__debounced__").concat(delay) }
    });
    return wrapped;
  }, __spreadArray([delay, maxWait], deps, true));
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useRafCallback/useRafCallback.js
var import_react3 = __toESM(require_react());

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/util/const.js
var noop = function() {
};
var isBrowser = typeof window !== "undefined" && typeof navigator !== "undefined" && typeof document !== "undefined";
var isStrictEqual = function(prev, next) {
  return prev === next;
};
var truthyAndArrayPredicate = function(conditions) {
  return conditions.every(function(i) {
    return Boolean(i);
  });
};
var truthyOrArrayPredicate = function(conditions) {
  return conditions.some(function(i) {
    return Boolean(i);
  });
};

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useRafCallback/useRafCallback.js
function useRafCallback(cb) {
  var cbRef = useSyncedRef(cb);
  var frame = (0, import_react3.useRef)(0);
  var cancel = (0, import_react3.useCallback)(function() {
    if (!isBrowser)
      return;
    if (frame.current) {
      cancelAnimationFrame(frame.current);
      frame.current = 0;
    }
  }, []);
  useUnmountEffect(cancel);
  return [
    (0, import_react3.useMemo)(function() {
      var wrapped = function() {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
          args[_i] = arguments[_i];
        }
        if (!isBrowser)
          return;
        cancel();
        frame.current = requestAnimationFrame(function() {
          cbRef.current.apply(cbRef, args);
          frame.current = 0;
        });
      };
      Object.defineProperties(wrapped, {
        length: { value: cb.length },
        name: { value: "".concat(cb.name || "anonymous", "__raf") }
      });
      return wrapped;
    }, []),
    cancel
  ];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useThrottledCallback/useThrottledCallback.js
var import_react4 = __toESM(require_react());
var __spreadArray2 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function useThrottledCallback(callback, deps, delay, noTrailing) {
  if (noTrailing === void 0) {
    noTrailing = false;
  }
  var timeout = (0, import_react4.useRef)();
  var lastCall = (0, import_react4.useRef)();
  useUnmountEffect(function() {
    if (timeout.current) {
      clearTimeout(timeout.current);
    }
  });
  return (0, import_react4.useMemo)(function() {
    var execute = function(context, args) {
      lastCall.current = void 0;
      callback.apply(context, args);
      timeout.current = setTimeout(function() {
        timeout.current = void 0;
        if (!noTrailing && lastCall.current) {
          execute(lastCall.current.this, lastCall.current.args);
          lastCall.current = void 0;
        }
      }, delay);
    };
    var wrapped = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      if (timeout.current) {
        lastCall.current = { args, this: this };
        return;
      }
      execute(this, args);
    };
    Object.defineProperties(wrapped, {
      length: { value: callback.length },
      name: { value: "".concat(callback.name || "anonymous", "__throttled__").concat(delay) }
    });
    return wrapped;
  }, __spreadArray2([delay, noTrailing], deps, true));
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useConditionalEffect/useConditionalEffect.js
var import_react5 = __toESM(require_react());
var __spreadArray3 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function useConditionalEffect(callback, deps, conditions, predicate, effectHook) {
  if (predicate === void 0) {
    predicate = truthyAndArrayPredicate;
  }
  if (effectHook === void 0) {
    effectHook = import_react5.useEffect;
  }
  var effectHookRestArgs = [];
  for (var _i = 5; _i < arguments.length; _i++) {
    effectHookRestArgs[_i - 5] = arguments[_i];
  }
  effectHook.apply(void 0, __spreadArray3([
    function() {
      if (predicate(conditions)) {
        return callback();
      }
    },
    deps
  ], effectHookRestArgs, false));
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useCustomCompareEffect/useCustomCompareEffect.js
var import_react6 = __toESM(require_react());

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/util/misc.js
function on(obj) {
  var args = [];
  for (var _i = 1; _i < arguments.length; _i++) {
    args[_i - 1] = arguments[_i];
  }
  if (obj && obj.addEventListener) {
    obj.addEventListener.apply(obj, args);
  }
}
function off(obj) {
  var args = [];
  for (var _i = 1; _i < arguments.length; _i++) {
    args[_i - 1] = arguments[_i];
  }
  if (obj && obj.removeEventListener) {
    obj.removeEventListener.apply(obj, args);
  }
}
var hasOwnProperty = function(obj, property) {
  return Object.prototype.hasOwnProperty.call(obj, property);
};
var yieldTrue = function() {
  return true;
};
var yieldFalse = function() {
  return false;
};
var basicDepsComparator = function(d1, d2) {
  if (d1 === d2)
    return true;
  if (d1.length !== d2.length)
    return false;
  for (var i = 0; i < d1.length; i++) {
    if (d1[i] !== d2[i]) {
      return false;
    }
  }
  return true;
};

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useCustomCompareEffect/useCustomCompareEffect.js
var __spreadArray4 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function useCustomCompareEffect(callback, deps, comparator, effectHook) {
  if (comparator === void 0) {
    comparator = basicDepsComparator;
  }
  if (effectHook === void 0) {
    effectHook = import_react6.useEffect;
  }
  var effectHookRestArgs = [];
  for (var _i = 4; _i < arguments.length; _i++) {
    effectHookRestArgs[_i - 4] = arguments[_i];
  }
  var dependencies = (0, import_react6.useRef)();
  if (dependencies.current === void 0 || isBrowser && !comparator(dependencies.current, deps)) {
    dependencies.current = deps;
  }
  effectHook.apply(void 0, __spreadArray4([callback, dependencies.current], effectHookRestArgs, false));
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useDebouncedEffect/useDebouncedEffect.js
var import_react7 = __toESM(require_react());
function useDebouncedEffect(callback, deps, delay, maxWait) {
  if (maxWait === void 0) {
    maxWait = 0;
  }
  (0, import_react7.useEffect)(useDebouncedCallback(callback, deps, delay, maxWait), deps);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useDeepCompareEffect/useDeepCompareEffect.js
var import_react8 = __toESM(require_react());

// node_modules/.pnpm/@react-hookz+deep-equal@1.0.1/node_modules/@react-hookz/deep-equal/esm/comparators.js
var compareDates = function(a, b) {
  return a.getTime() === b.getTime();
};
var compareRegexps = function(a, b) {
  return a.source === b.source && a.flags === b.flags;
};
var compareArrays = function(a, b, equal) {
  var l = a.length;
  if (l !== b.length)
    return false;
  while (l-- && equal(a[l], b[l]))
    ;
  return l === -1;
};
var compareMaps = function(a, b, equal) {
  if (a.size !== b.size)
    return false;
  var it = a.entries();
  var i;
  while (!(i = it.next()).done) {
    if (!b.has(i.value[0]) || !equal(i.value[1], b.get(i.value[0])))
      return false;
  }
  return true;
};
var compareSets = function(a, b) {
  if (a.size !== b.size)
    return false;
  var it = a.values();
  var i;
  while (!(i = it.next()).done) {
    if (!b.has(i.value))
      return false;
  }
  return true;
};
var compareDataViews = function(a, b) {
  var l = a.byteLength;
  if (l !== b.byteLength)
    return false;
  while (l-- && a.getInt8(l) === b.getInt8(l))
    ;
  return l === -1;
};
var compareArrayBuffers = function(a, b) {
  var l = a.length;
  if (l !== b.length)
    return false;
  while (l-- && a[l] === b[l])
    ;
  return l === -1;
};
var hasOwnProperty2 = Object.prototype.hasOwnProperty;
var oKeys = Object.keys;
var compareObjects = function(a, b, equal) {
  var i;
  var len = 0;
  for (i in a) {
    if (hasOwnProperty2.call(a, i)) {
      len++;
      if (!hasOwnProperty2.call(b, i))
        return false;
      if (!equal(a[i], b[i]))
        return false;
    }
  }
  return oKeys(b).length === len;
};

// node_modules/.pnpm/@react-hookz+deep-equal@1.0.1/node_modules/@react-hookz/deep-equal/esm/index.js
var _a = Object.prototype;
var valueOf = _a.valueOf;
var toString = _a.toString;
var isEqual = function(a, b) {
  if (a === b)
    return true;
  if (a && b && typeof a === "object" && typeof b === "object") {
    var ctor = a.constructor;
    if (ctor !== b.constructor)
      return false;
    if (ctor === Array)
      return compareArrays(a, b, isEqual);
    if (ctor === Date)
      return compareDates(a, b);
    if (ctor === RegExp)
      return compareRegexps(a, b);
    if (ctor === Map && a instanceof Map && b instanceof Map)
      return compareMaps(a, b, isEqual);
    if (ctor === Set && a instanceof Set && b instanceof Set)
      return compareSets(a, b);
    if (ctor === DataView)
      return compareDataViews(a, b);
    if (ctor === ArrayBuffer) {
      return compareArrayBuffers(new Uint8Array(a), new Uint8Array(b));
    }
    if (ArrayBuffer.isView(a) && ArrayBuffer.isView(b)) {
      return compareArrayBuffers(a, b);
    }
    if (a.valueOf !== valueOf)
      return a.valueOf() === b.valueOf();
    if (a.toString !== toString)
      return a.toString() === b.toString();
    return compareObjects(a, b, isEqual);
  }
  return a !== a && b !== b;
};

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useDeepCompareEffect/useDeepCompareEffect.js
var __spreadArray5 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function useDeepCompareEffect(callback, deps, effectHook) {
  if (effectHook === void 0) {
    effectHook = import_react8.useEffect;
  }
  var effectHookRestArgs = [];
  for (var _i = 3; _i < arguments.length; _i++) {
    effectHookRestArgs[_i - 3] = arguments[_i];
  }
  return useCustomCompareEffect.apply(void 0, __spreadArray5([callback, deps, isEqual, effectHook], effectHookRestArgs, false));
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useFirstMountState/useFirstMountState.js
var import_react9 = __toESM(require_react());
function useFirstMountState() {
  var isFirstMount = (0, import_react9.useRef)(true);
  (0, import_react9.useEffect)(function() {
    isFirstMount.current = false;
  }, []);
  return isFirstMount.current;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useIsMounted/useIsMounted.js
var import_react10 = __toESM(require_react());
function useIsMounted() {
  var isMounted = (0, import_react10.useRef)(false);
  var get = (0, import_react10.useCallback)(function() {
    return isMounted.current;
  }, []);
  (0, import_react10.useEffect)(function() {
    isMounted.current = true;
    return function() {
      isMounted.current = false;
    };
  }, []);
  return get;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useIsomorphicLayoutEffect/useIsomorphicLayoutEffect.js
var import_react11 = __toESM(require_react());
var useIsomorphicLayoutEffect = isBrowser ? import_react11.useLayoutEffect : import_react11.useEffect;

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useMountEffect/useMountEffect.js
var import_react12 = __toESM(require_react());
function useMountEffect(effect) {
  (0, import_react12.useEffect)(function() {
    effect();
  }, []);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useRafEffect/useRafEffect.js
var import_react13 = __toESM(require_react());
function useRafEffect(callback, deps) {
  var _a2 = useRafCallback(callback), rafCallback = _a2[0], cancelRaf = _a2[1];
  (0, import_react13.useEffect)(function() {
    rafCallback();
    return cancelRaf;
  }, deps);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useRerender/useRerender.js
var import_react14 = __toESM(require_react());
var stateChanger = function(state) {
  return !state;
};
function useRerender() {
  var _a2 = useSafeState(false), setState = _a2[1];
  return (0, import_react14.useCallback)(function() {
    setState(stateChanger);
  }, []);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useThrottledEffect/useThrottledEffect.js
var import_react15 = __toESM(require_react());
function useThrottledEffect(callback, deps, delay, noTrailing) {
  if (noTrailing === void 0) {
    noTrailing = false;
  }
  (0, import_react15.useEffect)(useThrottledCallback(callback, deps, delay, noTrailing), deps);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useUpdateEffect/useUpdateEffect.js
var import_react16 = __toESM(require_react());
function useUpdateEffect(effect, deps) {
  var isFirstMount = useFirstMountState();
  (0, import_react16.useEffect)(isFirstMount ? noop : effect, deps);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useDebouncedState/useDebouncedState.js
function useDebouncedState(initialState, delay, maxWait) {
  if (maxWait === void 0) {
    maxWait = 0;
  }
  var _a2 = useSafeState(initialState), state = _a2[0], setState = _a2[1];
  return [state, useDebouncedCallback(setState, [], delay, maxWait)];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useMap/useMap.js
var import_react17 = __toESM(require_react());
var proto = Map.prototype;
function useMap(entries) {
  var mapRef = (0, import_react17.useRef)();
  var rerender = useRerender();
  if (!mapRef.current) {
    var map_1 = new Map(entries);
    mapRef.current = map_1;
    map_1.set = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      proto.set.apply(map_1, args);
      rerender();
      return map_1;
    };
    map_1.clear = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      proto.clear.apply(map_1, args);
      rerender();
    };
    map_1.delete = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      var res = proto.delete.apply(map_1, args);
      rerender();
      return res;
    };
  }
  return mapRef.current;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useMediatedState/useMediatedState.js
var import_react18 = __toESM(require_react());

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/util/resolveHookState.js
function resolveHookState(nextState, prevState) {
  if (typeof nextState === "function")
    return nextState(prevState);
  return nextState;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useMediatedState/useMediatedState.js
function useMediatedState(initialState, mediator) {
  var _a2 = useSafeState(initialState), state = _a2[0], setState = _a2[1];
  var mediatorRef = useSyncedRef(mediator);
  return [
    state,
    (0, import_react18.useCallback)(function(value) {
      if (mediatorRef.current) {
        setState(function(prevState) {
          var _a3;
          return (_a3 = mediatorRef.current) === null || _a3 === void 0 ? void 0 : _a3.call(mediatorRef, resolveHookState(value, prevState));
        });
      } else {
        setState(value);
      }
    }, [])
  ];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/usePrevious/usePrevious.js
var import_react19 = __toESM(require_react());
function usePrevious(value) {
  var prev = (0, import_react19.useRef)();
  (0, import_react19.useEffect)(function() {
    prev.current = value;
  });
  return prev.current;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/usePreviousDistinct/usePreviousDistinct.js
var import_react20 = __toESM(require_react());
function usePreviousDistinct(value, predicate) {
  if (predicate === void 0) {
    predicate = isStrictEqual;
  }
  var _a2 = (0, import_react20.useState)(), previousState = _a2[0], setPreviousState = _a2[1];
  var currentRef = (0, import_react20.useRef)(value);
  useUpdateEffect(function() {
    if (!predicate(currentRef.current, value)) {
      setPreviousState(currentRef.current);
      currentRef.current = value;
    }
  }, [value]);
  return previousState;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useRafState/useRafState.js
function useRafState(initialState) {
  var _a2 = useSafeState(initialState), state = _a2[0], innerSetState = _a2[1];
  var _b = useRafCallback(innerSetState), setState = _b[0], cancelRaf = _b[1];
  useUnmountEffect(cancelRaf);
  return [state, setState];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useSafeState/useSafeState.js
var import_react21 = __toESM(require_react());
function useSafeState(initialState) {
  var _a2 = (0, import_react21.useState)(initialState), state = _a2[0], setState = _a2[1];
  var isMounted = useIsMounted();
  return [
    state,
    (0, import_react21.useCallback)(function(value) {
      if (isMounted())
        setState(value);
    }, [])
  ];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useSet/useSet.js
var import_react22 = __toESM(require_react());
var proto2 = Set.prototype;
function useSet(values) {
  var setRef = (0, import_react22.useRef)();
  var rerender = useRerender();
  if (!setRef.current) {
    var set_1 = new Set(values);
    setRef.current = set_1;
    set_1.add = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      proto2.add.apply(set_1, args);
      rerender();
      return set_1;
    };
    set_1.clear = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      proto2.clear.apply(set_1, args);
      rerender();
    };
    set_1.delete = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      var res = proto2.delete.apply(set_1, args);
      rerender();
      return res;
    };
  }
  return setRef.current;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useToggle/useToggle.js
var import_react23 = __toESM(require_react());
function useToggle(initialState) {
  if (initialState === void 0) {
    initialState = false;
  }
  var _a2 = useSafeState(initialState), state = _a2[0], setState = _a2[1];
  return [
    state,
    (0, import_react23.useCallback)(function(nextState) {
      setState(function(prevState) {
        if (typeof nextState === "undefined") {
          return !prevState;
        }
        return Boolean(resolveHookState(nextState, prevState));
      });
    }, [])
  ];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useThrottledState/useThrottledState.js
function useThrottledState(initialState, delay, noTrailing) {
  if (noTrailing === void 0) {
    noTrailing = false;
  }
  var _a2 = useSafeState(initialState), state = _a2[0], setState = _a2[1];
  return [state, useThrottledCallback(setState, [], delay, noTrailing)];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useValidator/useValidator.js
var import_react24 = __toESM(require_react());
function useValidator(validator, deps, initialValidity) {
  if (initialValidity === void 0) {
    initialValidity = { isValid: void 0 };
  }
  var _a2 = useSafeState(initialValidity), validity = _a2[0], setValidity = _a2[1];
  var validatorRef = useSyncedRef(function() {
    if (validator.length) {
      validator(setValidity);
    } else {
      setValidity(validator());
    }
  });
  (0, import_react24.useEffect)(function() {
    validatorRef.current();
  }, deps);
  return [
    validity,
    (0, import_react24.useCallback)(function() {
      validatorRef.current();
    }, [])
  ];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useNetworkState/useNetworkState.js
var import_react25 = __toESM(require_react());
var navigator2 = isBrowser ? window.navigator : void 0;
var conn = navigator2 && (navigator2.connection || navigator2.mozConnection || navigator2.webkitConnection);
function getConnectionState(previousState) {
  var online = navigator2 === null || navigator2 === void 0 ? void 0 : navigator2.onLine;
  var previousOnline = previousState === null || previousState === void 0 ? void 0 : previousState.online;
  return {
    online,
    previous: previousOnline,
    since: online !== previousOnline ? new Date() : previousState === null || previousState === void 0 ? void 0 : previousState.since,
    downlink: conn === null || conn === void 0 ? void 0 : conn.downlink,
    downlinkMax: conn === null || conn === void 0 ? void 0 : conn.downlinkMax,
    effectiveType: conn === null || conn === void 0 ? void 0 : conn.effectiveType,
    rtt: conn === null || conn === void 0 ? void 0 : conn.rtt,
    saveData: conn === null || conn === void 0 ? void 0 : conn.saveData,
    type: conn === null || conn === void 0 ? void 0 : conn.type
  };
}
function useNetworkState(initialState) {
  var _a2 = useSafeState(initialState !== null && initialState !== void 0 ? initialState : getConnectionState), state = _a2[0], setState = _a2[1];
  (0, import_react25.useEffect)(function() {
    var handleStateChange = function() {
      setState(getConnectionState);
    };
    on(window, "online", handleStateChange, { passive: true });
    on(window, "offline", handleStateChange, { passive: true });
    if (conn) {
      on(conn, "change", handleStateChange, { passive: true });
    }
    return function() {
      off(window, "online", handleStateChange);
      off(window, "offline", handleStateChange);
      if (conn) {
        off(conn, "change", handleStateChange);
      }
    };
  }, []);
  return state;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/usePermission/usePermission.js
var import_react26 = __toESM(require_react());
function usePermission(descriptor) {
  var _a2 = useSafeState("not-requested"), state = _a2[0], setState = _a2[1];
  (0, import_react26.useEffect)(function() {
    var unmount = { current: null };
    setState("requested");
    navigator.permissions.query(descriptor).then(function(status) {
      var handleChange = function() {
        setState(status.state);
      };
      setState(status.state);
      on(status, "change", handleChange, { passive: true });
      unmount.current = function() {
        off(status, "change", handleChange);
      };
    });
    return function() {
      if (unmount.current) {
        unmount.current();
      }
    };
  }, [descriptor.name]);
  return state;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useSyncedRef/useSyncedRef.js
var import_react27 = __toESM(require_react());
function useSyncedRef(value) {
  var ref = (0, import_react27.useRef)(value);
  ref.current = value;
  return (0, import_react27.useMemo)(function() {
    return Object.freeze({
      get current() {
        return ref.current;
      }
    });
  }, []);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useStorageValue/useStorageValue.js
var import_react28 = __toESM(require_react());
function useStorageValue(storage, key, defaultValue, options) {
  if (defaultValue === void 0) {
    defaultValue = null;
  }
  if (options === void 0) {
    options = {};
  }
  var isolated = options.isolated;
  var _a2 = options.initializeWithStorageValue, initializeWithStorageValue = _a2 === void 0 ? true : _a2, _b = options.handleStorageEvent, handleStorageEvent = _b === void 0 ? true : _b, _c = options.storeDefaultValue, storeDefaultValue = _c === void 0 ? false : _c;
  if (!isBrowser) {
    storeDefaultValue = false;
    initializeWithStorageValue = false;
    handleStorageEvent = false;
  }
  var methods = useSyncedRef({
    fetchVal: function() {
      return parse(storage.getItem(key), defaultValue);
    },
    storeVal: function(val) {
      var stringified = stringify(val);
      if (stringified) {
        storage.setItem(key, stringified);
        return true;
      }
      return false;
    },
    removeVal: function() {
      storage.removeItem(key);
    },
    setVal: function(val) {
      setState(parse(val, defaultValue));
    },
    fetchState: function() {
      var newVal = methods.current.fetchVal();
      setState(newVal);
      return newVal !== stateRef.current ? newVal : null;
    },
    setState: function(nextState) {
      setState(nextState === null ? defaultValue : nextState);
    }
  });
  var isFirstMount = useFirstMountState();
  var _d = useSafeState(initializeWithStorageValue && isFirstMount ? methods.current.fetchVal() : void 0), state = _d[0], setState = _d[1];
  var prevState = usePrevious(state);
  var stateRef = useSyncedRef(state);
  var keyRef = useSyncedRef(key);
  var isolatedRef = useSyncedRef(isolated);
  useMountEffect(function() {
    if (!initializeWithStorageValue) {
      methods.current.fetchState();
    }
  });
  useConditionalEffect(function() {
    methods.current.storeVal(defaultValue);
  }, void 0, [prevState !== state, storeDefaultValue && state === defaultValue && defaultValue !== null]);
  useUpdateEffect(function() {
    methods.current.fetchState();
  }, [key]);
  useIsomorphicLayoutEffect(function() {
    if (!handleStorageEvent)
      return;
    var storageHandler = function(ev) {
      if (ev.storageArea !== storage)
        return;
      if (ev.key !== keyRef.current)
        return;
      methods.current.setVal(ev.newValue);
    };
    on(window, "storage", storageHandler, { passive: true });
    return function() {
      off(window, "storage", storageHandler);
    };
  }, [handleStorageEvent]);
  useIsomorphicLayoutEffect(function() {
    if (isolated)
      return;
    var storageKeys = storageKeysUsed.get(storage);
    if (!storageKeys) {
      storageKeys = /* @__PURE__ */ new Map();
      storageKeysUsed.set(storage, storageKeys);
    }
    var keySetters = storageKeys.get(key);
    if (!keySetters) {
      keySetters = /* @__PURE__ */ new Set();
      storageKeys.set(key, keySetters);
    }
    var mSetState = methods.current.setState;
    keySetters.add(mSetState);
    return function() {
      keySetters === null || keySetters === void 0 ? void 0 : keySetters.delete(mSetState);
    };
  }, [isolated, key]);
  return [
    state,
    (0, import_react28.useCallback)(function(newState) {
      var _a3, _b2;
      if (!isBrowser)
        return;
      var s = resolveHookState(newState, stateRef.current);
      if (methods.current.storeVal(s)) {
        methods.current.setState(s);
        if (!isolatedRef.current) {
          (_b2 = (_a3 = storageKeysUsed.get(storage)) === null || _a3 === void 0 ? void 0 : _a3.get(keyRef.current)) === null || _b2 === void 0 ? void 0 : _b2.forEach(function(setter) {
            if (setter === methods.current.setState)
              return;
            setter(s);
          });
        }
      }
    }, []),
    (0, import_react28.useCallback)(function() {
      var _a3, _b2;
      if (!isBrowser)
        return;
      methods.current.removeVal();
      methods.current.setState(null);
      if (!isolatedRef.current) {
        (_b2 = (_a3 = storageKeysUsed.get(storage)) === null || _a3 === void 0 ? void 0 : _a3.get(keyRef.current)) === null || _b2 === void 0 ? void 0 : _b2.forEach(function(setter) {
          if (setter === methods.current.setState)
            return;
          setter(null);
        });
      }
    }, []),
    (0, import_react28.useCallback)(function() {
      var _a3, _b2;
      if (!isBrowser)
        return;
      var newVal = methods.current.fetchState();
      if (newVal !== null && !isolatedRef.current) {
        (_b2 = (_a3 = storageKeysUsed.get(storage)) === null || _a3 === void 0 ? void 0 : _a3.get(keyRef.current)) === null || _b2 === void 0 ? void 0 : _b2.forEach(function(setter) {
          if (setter === methods.current.setState)
            return;
          setter(newVal);
        });
      }
    }, [])
  ];
}
var storageKeysUsed = /* @__PURE__ */ new Map();
var stringify = function(data) {
  if (data === null) {
    console.warn("'null' is not a valid data for useStorageValue hook, this operation will take no effect");
    return null;
  }
  try {
    return JSON.stringify(data);
  } catch (error) {
    console.warn(error);
    return null;
  }
};
var parse = function(str, fallback) {
  if (str === null)
    return fallback;
  try {
    return JSON.parse(str);
  } catch (error) {
    console.warn(error);
    return fallback;
  }
};

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useLocalStorageValue/useLocalStorageValue.js
var IS_LOCAL_STORAGE_AVAILABLE = false;
try {
  IS_LOCAL_STORAGE_AVAILABLE = isBrowser && !!window.localStorage;
} catch (_a2) {
  IS_LOCAL_STORAGE_AVAILABLE = false;
}
var useLocalStorageValue = IS_LOCAL_STORAGE_AVAILABLE ? function(key, defaultValue, options) {
  if (defaultValue === void 0) {
    defaultValue = null;
  }
  if (options === void 0) {
    options = {};
  }
  return useStorageValue(localStorage, key, defaultValue, options);
} : function(key, defaultValue, options) {
  if (defaultValue === void 0) {
    defaultValue = null;
  }
  if (options === void 0) {
    options = {};
  }
  if (isBrowser && true) {
    console.warn("LocalStorage is not available in this environment");
  }
  return [void 0, noop, noop, noop];
};

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useSessionStorageValue/useSessionStorageValue.js
var IS_SESSION_STORAGE_AVAILABLE = false;
try {
  IS_SESSION_STORAGE_AVAILABLE = isBrowser && !!window.sessionStorage;
} catch (_a2) {
  IS_SESSION_STORAGE_AVAILABLE = false;
}
var useSessionStorageValue = IS_SESSION_STORAGE_AVAILABLE ? function(key, defaultValue, options) {
  if (defaultValue === void 0) {
    defaultValue = null;
  }
  if (options === void 0) {
    options = {};
  }
  return useStorageValue(sessionStorage, key, defaultValue, options);
} : function(key, defaultValue, options) {
  if (defaultValue === void 0) {
    defaultValue = null;
  }
  if (options === void 0) {
    options = {};
  }
  if (isBrowser && true) {
    console.warn("SessionStorage is not available in this environment");
  }
  return [void 0, noop, noop, noop];
};

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useAsync/useAsync.js
var import_react29 = __toESM(require_react());
var __assign = function() {
  __assign = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign.apply(this, arguments);
};
function useAsync(asyncFn, initialValue) {
  var _a2 = useSafeState({
    status: "not-executed",
    error: void 0,
    result: initialValue
  }), state = _a2[0], setState = _a2[1];
  var promiseRef = (0, import_react29.useRef)();
  var argsRef = (0, import_react29.useRef)();
  var methods = useSyncedRef({
    execute: function() {
      var params = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        params[_i] = arguments[_i];
      }
      argsRef.current = params;
      var promise = asyncFn.apply(void 0, params);
      promiseRef.current = promise;
      setState(function(s) {
        return __assign(__assign({}, s), { status: "loading" });
      });
      promise.then(function(result) {
        if (promise === promiseRef.current) {
          setState(function(s) {
            return __assign(__assign({}, s), { status: "success", error: void 0, result });
          });
        }
      }, function(error) {
        if (promise === promiseRef.current) {
          setState(function(s) {
            return __assign(__assign({}, s), { status: "error", error });
          });
        }
      });
      return promise;
    },
    reset: function() {
      setState({
        status: "not-executed",
        error: void 0,
        result: initialValue
      });
      promiseRef.current = void 0;
      argsRef.current = void 0;
    }
  });
  return [
    state,
    (0, import_react29.useMemo)(function() {
      return {
        reset: function() {
          methods.current.reset();
        },
        execute: function() {
          var _a3;
          var params = [];
          for (var _i = 0; _i < arguments.length; _i++) {
            params[_i] = arguments[_i];
          }
          return (_a3 = methods.current).execute.apply(_a3, params);
        }
      };
    }, []),
    { promise: promiseRef.current, lastArgs: argsRef.current }
  ];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useAsyncAbortable/useAsyncAbortable.js
var import_react30 = __toESM(require_react());
var __assign2 = function() {
  __assign2 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign2.apply(this, arguments);
};
var __awaiter = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1)
      throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f)
      throw new TypeError("Generator is already executing.");
    while (_)
      try {
        if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done)
          return t;
        if (y = 0, t)
          op = [op[0] & 2, t.value];
        switch (op[0]) {
          case 0:
          case 1:
            t = op;
            break;
          case 4:
            _.label++;
            return { value: op[1], done: false };
          case 5:
            _.label++;
            y = op[1];
            op = [0];
            continue;
          case 7:
            op = _.ops.pop();
            _.trys.pop();
            continue;
          default:
            if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _ = 0;
              continue;
            }
            if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
              _.label = op[1];
              break;
            }
            if (op[0] === 6 && _.label < t[1]) {
              _.label = t[1];
              t = op;
              break;
            }
            if (t && _.label < t[2]) {
              _.label = t[2];
              _.ops.push(op);
              break;
            }
            if (t[2])
              _.ops.pop();
            _.trys.pop();
            continue;
        }
        op = body.call(thisArg, _);
      } catch (e) {
        op = [6, e];
        y = 0;
      } finally {
        f = t = 0;
      }
    if (op[0] & 5)
      throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var __spreadArray6 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function useAsyncAbortable(asyncFn, initialValue) {
  var _this = this;
  var abortController = (0, import_react30.useRef)();
  var fn = function() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    return __awaiter(_this, void 0, void 0, function() {
      var ac;
      var _a3;
      return __generator(this, function(_b) {
        (_a3 = abortController.current) === null || _a3 === void 0 ? void 0 : _a3.abort();
        ac = new AbortController();
        abortController.current = ac;
        return [2, asyncFn.apply(void 0, __spreadArray6([ac.signal], args, false)).finally(function() {
          if (abortController.current === ac) {
            abortController.current = void 0;
          }
        })];
      });
    });
  };
  var _a2 = useAsync(fn, initialValue), state = _a2[0], asyncActions = _a2[1], asyncMeta = _a2[2];
  return [
    state,
    (0, import_react30.useMemo)(function() {
      var actions = {
        reset: function() {
          actions.abort();
          asyncActions.reset();
        },
        abort: function() {
          var _a3;
          (_a3 = abortController.current) === null || _a3 === void 0 ? void 0 : _a3.abort();
        }
      };
      return __assign2(__assign2({}, asyncActions), actions);
    }, []),
    __assign2(__assign2({}, asyncMeta), { abortController: abortController.current })
  ];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useIntersectionObserver/useIntersectionObserver.js
var import_react31 = __toESM(require_react());
var __spreadArray7 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var DEFAULT_THRESHOLD = [0];
var DEFAULT_ROOT_MARGIN = "0px";
var observers = /* @__PURE__ */ new Map();
var getObserverEntry = function(options) {
  var _a2;
  var root = (_a2 = options.root) !== null && _a2 !== void 0 ? _a2 : document;
  var rootObservers = observers.get(root);
  if (!rootObservers) {
    rootObservers = /* @__PURE__ */ new Map();
    observers.set(root, rootObservers);
  }
  var opt = JSON.stringify([options.rootMargin, options.threshold]);
  var entry = rootObservers.get(opt);
  if (!entry) {
    var callbacks_1 = /* @__PURE__ */ new Map();
    var observer_1 = new IntersectionObserver(function(entries) {
      return entries.forEach(function(e) {
        var _a3;
        return (_a3 = callbacks_1.get(e.target)) === null || _a3 === void 0 ? void 0 : _a3.forEach(function(cb) {
          return setTimeout(function() {
            return cb(e);
          }, 0);
        });
      });
    }, options);
    entry = {
      observer: observer_1,
      observe: function(target, callback) {
        var cbs = callbacks_1.get(target);
        if (!cbs) {
          cbs = /* @__PURE__ */ new Set();
          callbacks_1.set(target, cbs);
          observer_1.observe(target);
        }
        cbs.add(callback);
      },
      unobserve: function(target, callback) {
        var cbs = callbacks_1.get(target);
        if (cbs) {
          cbs.delete(callback);
          if (!cbs.size) {
            callbacks_1.delete(target);
            observer_1.unobserve(target);
            if (!callbacks_1.size) {
              observer_1.disconnect();
              rootObservers.delete(opt);
              if (!rootObservers.size) {
                observers.delete(root);
              }
            }
          }
        }
      }
    };
    rootObservers.set(opt, entry);
  }
  return entry;
};
function useIntersectionObserver(target, _a2) {
  var _b = _a2 === void 0 ? {} : _a2, _c = _b.threshold, threshold = _c === void 0 ? DEFAULT_THRESHOLD : _c, r = _b.root, _d = _b.rootMargin, rootMargin = _d === void 0 ? DEFAULT_ROOT_MARGIN : _d;
  var _e = useSafeState(), state = _e[0], setState = _e[1];
  (0, import_react31.useEffect)(function() {
    var tgt = target && "current" in target ? target.current : target;
    if (!tgt)
      return;
    var subscribed = true;
    var observerEntry = getObserverEntry({
      root: r && "current" in r ? r.current : r,
      rootMargin,
      threshold
    });
    var handler = function(entry) {
      if (subscribed) {
        setState(entry);
      }
    };
    observerEntry.observe(tgt, handler);
    return function() {
      subscribed = false;
      observerEntry.unobserve(tgt, handler);
    };
  }, __spreadArray7([target, r, rootMargin], threshold, true));
  return state;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useResizeObserver/useResizeObserver.js
var import_react32 = __toESM(require_react());
var observerSingleton;
function getResizeObserver() {
  if (!isBrowser)
    return void 0;
  if (observerSingleton)
    return observerSingleton;
  var callbacks = /* @__PURE__ */ new Map();
  var observer = new ResizeObserver(function(entries) {
    entries.forEach(function(entry) {
      var _a2;
      return (_a2 = callbacks.get(entry.target)) === null || _a2 === void 0 ? void 0 : _a2.forEach(function(cb) {
        return setTimeout(function() {
          return cb(entry);
        }, 0);
      });
    });
  });
  observerSingleton = {
    observer,
    subscribe: function(target, callback) {
      var cbs = callbacks.get(target);
      if (!cbs) {
        cbs = /* @__PURE__ */ new Set();
        callbacks.set(target, cbs);
        observer.observe(target);
      }
      cbs.add(callback);
    },
    unsubscribe: function(target, callback) {
      var cbs = callbacks.get(target);
      if (cbs) {
        cbs.delete(callback);
        if (!cbs.size) {
          callbacks.delete(target);
          observer.unobserve(target);
        }
      }
    }
  };
  return observerSingleton;
}
function useResizeObserver(target, callback, enabled) {
  if (enabled === void 0) {
    enabled = true;
  }
  var ro = enabled && getResizeObserver();
  var cb = useSyncedRef(callback);
  (0, import_react32.useEffect)(function() {
    if (!ro)
      return;
    var subscribed = true;
    var tgt = target && "current" in target ? target.current : target;
    if (!tgt)
      return;
    var handler = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      if (subscribed) {
        cb.current.apply(cb, args);
      }
    };
    ro.subscribe(tgt, handler);
    return function() {
      subscribed = false;
      ro.unsubscribe(tgt, handler);
    };
  }, [target, ro]);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useMeasure/useMeasure.js
var import_react33 = __toESM(require_react());
function useMeasure(enabled) {
  if (enabled === void 0) {
    enabled = true;
  }
  var elementRef = (0, import_react33.useRef)(null);
  var _a2 = useSafeState(), rect = _a2[0], setRect = _a2[1];
  var observerHandler = useRafCallback(function(entry) {
    return setRect(entry.contentRect);
  })[0];
  useResizeObserver(elementRef, observerHandler, enabled);
  return [rect, elementRef];
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useMediaQuery/useMediaQuery.js
var import_react34 = __toESM(require_react());
var queriesMap = /* @__PURE__ */ new Map();
var querySubscribe = function(query, setState) {
  var entry = queriesMap.get(query);
  if (!entry) {
    var mql_1 = matchMedia(query);
    var dispatchers_1 = /* @__PURE__ */ new Set();
    var listener = function() {
      dispatchers_1.forEach(function(d) {
        return d(mql_1.matches);
      });
    };
    if (mql_1.addEventListener)
      mql_1.addEventListener("change", listener, { passive: true });
    else
      mql_1.addListener(listener);
    entry = {
      mql: mql_1,
      dispatchers: dispatchers_1,
      listener
    };
    queriesMap.set(query, entry);
  }
  entry.dispatchers.add(setState);
  setState(entry.mql.matches, true);
};
var queryUnsubscribe = function(query, setState) {
  var entry = queriesMap.get(query);
  if (entry) {
    var mql = entry.mql, dispatchers = entry.dispatchers, listener = entry.listener;
    dispatchers.delete(setState);
    if (!dispatchers.size) {
      queriesMap.delete(query);
      if (mql.removeEventListener)
        mql.removeEventListener("change", listener);
      else
        mql.removeListener(listener);
    }
  }
};
function useMediaQuery(query, matchOnMount) {
  var rerender = useRerender();
  var previousQuery = usePrevious(query);
  var state = (0, import_react34.useRef)();
  var setState = (0, import_react34.useCallback)(function(matches, initial) {
    if (state.current !== matches) {
      state.current = matches;
      if (!initial || matchOnMount)
        rerender();
    }
  }, [matchOnMount]);
  if (!matchOnMount && isBrowser && previousQuery !== query) {
    querySubscribe(query, setState);
  }
  (0, import_react34.useEffect)(function() {
    if (matchOnMount) {
      querySubscribe(query, setState);
    }
    return function() {
      return queryUnsubscribe(query, setState);
    };
  }, [query]);
  return state.current;
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useKeyboardEvent/useKeyboardEvent.js
var import_react35 = __toESM(require_react());
var createKeyPredicate = function(keyFilter) {
  if (typeof keyFilter === "function")
    return keyFilter;
  if (typeof keyFilter === "string")
    return function(ev) {
      return ev.key === keyFilter;
    };
  return keyFilter ? yieldTrue : yieldFalse;
};
var WINDOW_OR_NULL = isBrowser ? window : null;
function useKeyboardEvent(keyOrPredicate, callback, deps, options) {
  if (options === void 0) {
    options = {};
  }
  var _a2 = options.event, event = _a2 === void 0 ? "keydown" : _a2, _b = options.target, target = _b === void 0 ? WINDOW_OR_NULL : _b, eventOptions = options.eventOptions;
  var cbRef = useSyncedRef(callback);
  var handler = (0, import_react35.useMemo)(function() {
    var predicate = createKeyPredicate(keyOrPredicate);
    return function kbEventHandler(ev) {
      if (predicate(ev)) {
        cbRef.current.call(this, ev);
      }
    };
  }, deps);
  useEventListener(target, event, handler, eventOptions);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useScreenOrientation/useScreenOrientation.js
function useScreenOrientation() {
  return useMediaQuery("(orientation: portrait)") ? "portrait" : "landscape";
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useClickOutside/useClickOutside.js
var import_react36 = __toESM(require_react());
var __spreadArray8 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var DEFAULT_EVENTS = ["mousedown", "touchstart"];
function useClickOutside(ref, callback, events) {
  if (events === void 0) {
    events = DEFAULT_EVENTS;
  }
  var cbRef = useSyncedRef(callback);
  var refRef = useSyncedRef(ref);
  (0, import_react36.useEffect)(function() {
    function handler(event) {
      if (!refRef.current.current)
        return;
      var evtTarget = event.target;
      var cb = cbRef.current;
      if (!evtTarget || !!evtTarget && !refRef.current.current.contains(evtTarget)) {
        cb.call(this, event);
      }
    }
    events.forEach(function(name) {
      return on(document, name, handler, { passive: true });
    });
    return function() {
      events.forEach(function(name) {
        return off(document, name, handler, { passive: true });
      });
    };
  }, __spreadArray8([], events, true));
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useDocumentTitle/useDocumentTitle.js
var import_react37 = __toESM(require_react());
function useDocumentTitle(title, options) {
  if (options === void 0) {
    options = {};
  }
  var titleRef = (0, import_react37.useRef)(isBrowser ? document.title : "");
  var optionsRef = useSyncedRef(options);
  (0, import_react37.useEffect)(function() {
    document.title = options.wrapper ? options.wrapper(title) : title;
  }, [title, options.wrapper]);
  useUnmountEffect(function() {
    if (optionsRef.current.restoreOnUnmount) {
      document.title = titleRef.current;
    }
  });
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useEventListener/useEventListener.js
var import_react38 = __toESM(require_react());
var __spreadArray9 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function useEventListener(target) {
  var params = [];
  for (var _i = 1; _i < arguments.length; _i++) {
    params[_i - 1] = arguments[_i];
  }
  var isMounted = useIsMounted();
  var listenerRef = useSyncedRef(params[1]);
  var eventListener = (0, import_react38.useMemo)(function() {
    return function() {
      var args = [];
      for (var _i2 = 0; _i2 < arguments.length; _i2++) {
        args[_i2] = arguments[_i2];
      }
      if (!isMounted())
        return;
      if (typeof listenerRef.current === "function") {
        listenerRef.current.apply(this, args);
      } else if (typeof listenerRef.current.handleEvent === "function") {
        listenerRef.current.handleEvent.apply(this, args);
      }
    };
  }, []);
  (0, import_react38.useEffect)(function() {
    var tgt = target && hasOwnProperty(target, "current") ? target.current : target;
    if (!tgt)
      return;
    var restParams = params.slice(2);
    tgt.addEventListener.apply(tgt, __spreadArray9([params[0], eventListener], restParams, false));
    return function() {
      return tgt.removeEventListener.apply(tgt, __spreadArray9([params[0], eventListener], restParams, false));
    };
  }, [target, params[0]]);
}

// node_modules/.pnpm/@react-hookz+web@13.2.0_react-dom@18.0.0+react@18.0.0/node_modules/@react-hookz/web/esm/useWindowSize/useWindowSize.js
var import_react39 = __toESM(require_react());
var listeners = /* @__PURE__ */ new Set();
var callAllListeners = function() {
  listeners.forEach(function(l) {
    l({
      width: window.innerWidth,
      height: window.innerHeight
    });
  });
};
function useWindowSize(stateHook, measureOnMount) {
  if (stateHook === void 0) {
    stateHook = useRafState;
  }
  var isFirstMount = useFirstMountState();
  var _a2 = stateHook({
    width: isFirstMount && isBrowser && !measureOnMount ? window.innerWidth : 0,
    height: isFirstMount && isBrowser && !measureOnMount ? window.innerHeight : 0
  }), size = _a2[0], setSize = _a2[1];
  useMountEffect(function() {
    if (measureOnMount) {
      setSize({
        width: window.innerWidth,
        height: window.innerHeight
      });
    }
  });
  (0, import_react39.useEffect)(function() {
    if (listeners.size === 0) {
      window.addEventListener("resize", callAllListeners, { passive: true });
    }
    listeners.add(setSize);
    return function() {
      listeners.delete(setSize);
      if (listeners.size === 0) {
        window.removeEventListener("resize", callAllListeners);
      }
    };
  }, [setSize]);
  return size;
}
export {
  truthyAndArrayPredicate,
  truthyOrArrayPredicate,
  useAsync,
  useAsyncAbortable,
  useClickOutside,
  useConditionalEffect,
  useCustomCompareEffect,
  useDebouncedCallback,
  useDebouncedEffect,
  useDebouncedState,
  useDeepCompareEffect,
  useDocumentTitle,
  useEventListener,
  useFirstMountState,
  useIntersectionObserver,
  useIsMounted,
  useIsomorphicLayoutEffect,
  useKeyboardEvent,
  useLocalStorageValue,
  useMap,
  useMeasure,
  useMediaQuery,
  useMediatedState,
  useMountEffect,
  useNetworkState,
  usePermission,
  usePrevious,
  usePreviousDistinct,
  useRafCallback,
  useRafEffect,
  useRafState,
  useRerender,
  useResizeObserver,
  useSafeState,
  useScreenOrientation,
  useSessionStorageValue,
  useSet,
  useSyncedRef,
  useThrottledCallback,
  useThrottledEffect,
  useThrottledState,
  useToggle,
  useUnmountEffect,
  useUpdateEffect,
  useValidator,
  useWindowSize
};
//# sourceMappingURL=@react-hookz_web_esm.js.map
